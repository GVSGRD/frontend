import { alpha } from '@mui/material/styles';

export const navigationCustomizations = {
  MuiLink: {
    styleOverrides: {
      root: {
        fontWeight: 500,
        textDecoration: 'none',
      },
    },
  },
};